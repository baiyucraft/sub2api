# 专属codex 请求分配变化只读审计

## 此用户优先跟进：429 减量触发（2026-09-19）

针对 `1933254433@qq.com` 的“专属codex”（Key ID 5861）单独跟进。工作假设是“对方收到 429 就减量”，尚未证实其实际调度规则，也不代表对方收到 503 不会减量。

- **普通账号：已有修复，线上启用及效果待核验。** OpenAI 账号本地等待队列满时，可在当前请求内排除账号及共享并发池并重新选号；换号成功则不向用户返回该次排队满 429，容量耗尽默认返回 503。仅覆盖符合换号条件的本地账号排队满，不代表所有 429 均已修复。
- **上游账号：已实现复用普通账号容量故障转移设置，尚未发布生产。** 池模式继续先做同账号重试，再按物理账号换号；开启 `gateway_capacity_failover_settings` 后，上游绑定账号的 429 使用独立 `max_switches` 预算，全部候选或预算耗尽时返回配置的容量耗尽状态（默认 503、`gateway_capacity_exhausted`），Ops 仍保留真实上游 429。设置关闭时保持原最终 429。

本地验证已通过 429 定向测试、完整 handler/service 包测试、后台设置前端测试与 TypeScript 类型检查。完整 `go test ./...` 和 unit-tag 测试仍被当前工作区另一项 Codex STATE 改动的既有 repository SQL mock 列数不一致阻断（mock 预期 9 列、实现扫描 10 列），与本次 429 变更路径无关，发布前仍需由该功能所有者收口。

后续发布后优先按此 Key 核对最终对外 429（尤其 `Too many pending requests`）是否减少、内部换号是否成功、容量耗尽 503 是否出现，以及随后的请求量变化。本次只完成代码与测试，不修改生产配置。

> 历史分析快照：2026-09-19。账号 `1933254433@qq.com`（用户 ID 135），Key“专属codex”（Key ID 5861）。后续引用时请保留统计窗口与估算口径，不将本报告作为实时状态。
>
> 可复用统计摘要：[codex-key-traffic-audit-2026-09-19.summary.json](codex-key-traffic-audit-2026-09-19.summary.json)。摘要不含 API Key 凭证、请求正文或连接信息。
>
> 后续补充：[成功业务首字与加量、减量](codex-key-traffic-audit-2026-09-19-ttft.md)。沿用同一截止时间的脱敏数据：未发现稳定的单次首字阈值；持续慢首字与错误、拥堵混杂，不能单独据此判断对方调配策略。原有修复跟进保持不变。

## 范围和结论

- 对象：用户 `1933254433@qq.com`，API Key 名称“专属codex”，内部 ID 5861，分组“gpt-专属”。未读取或导出 API Key 凭证。
- 查询窗口：2026-09-12 21:57:55 至 2026-09-19 21:57:55，北京时间。
- Key 创建于 2026-09-16 17:10:30，因此只能观察约 3 天 5 小时，不能代表完整一周行为。
- 生产数据库只读；未修改账号、限流、调度、数据库或代码，未主动调用模型。

结论：多模型短探测与流量启动/恢复有一定关联；排队类限流错误与降量的关联比一般503过载错误更明显。但是成功探针、429和其他错误都存在反例，不能据此还原对方的内部调度算法。

## 样本与统计口径

- usage：48,873 条。
- ops error：3,828 条，其中 2,450 条明确标注 Recovered，记录状态为200；2,438条能关联usage，另12条不能关联。
- 最终失败记录：1,360条；另18条为客户端取消499，不作为服务器返回错误处理。
- 合并关联记录后约50,263条观测记录；这不是所有入口请求的绝对精确总数。原查询中usage请求ID和error请求ID分别唯一，但失败在不同请求ID重试仍按独立到站尝试观察。
- 窄口径排除575条疑似探针：Kuma323、Go非流式76 input token 211、Go非流式7 input token 41。
- 排除探针后49,688条业务观测，其中49,660条可重建开始时间；其余28条只保留结束/错误事件，不编造时延。
- 探针敏感性分析额外排除159条Go客户端、4,000–4,600 input token、低输出的疑似填充探针，总排除734条。该分类不能证明真实请求用途。

usage.created_at是异步记账时间，duration仅包括最后一次Forward；错误区间只对四段时延齐全、非负且总和不超过一小时的记录重建。因此“请求到达率”“平均在途数”均为估算，后者不是对方设置的并发上限。对错误事件的比较排除触发请求自身，避免其完成释放并发造成机械性下降。

错误记录中的429/502/503可能是SSE流内失败映射的状态，而实际HTTP已经是200；没有逐请求access日志，不能保证线上HTTP状态码与记录相同。

## 哪些探测与加量有关

### 多模型7-token短探测

41条请求按相邻完成间隔不超过180秒归并为17批；前后15分钟均完整的有15批：10批业务到达数增加、5批减少，中位变化约+3.8次/分钟。批次仍可能受到总体需求和其他事件影响，不能作为独立因果实验。

最清楚的恢复样本：

1. 9月17日01:12:36至08:25:23没有可识别的业务新请求，停流前10分钟没有最终错误。
2. 08:24:36、08:24:38、08:24:57先后记录Go客户端非流式7-token请求，模型为gpt-5.5、gpt-6-astra、gpt-5.6-sol。
3. 08:25:23业务恢复。
4. 以该批探测前后15分钟比较，业务量0→71条，估算平均在途0→1.84。

反例：9月17日09:25相似三模型探测批次，业务539→531条；9月18日12:49相似批次，214→151条。因此它更像启动/恢复时的检查信号，而不是必然加量指令。

### 常规周期探测

| 特征 | 数量 | 间隔特征 | 成功记录后5分钟业务量变化 |
|---|---:|---|---|
| Uptime-Kuma，gpt-5.5 | 323 | 完成间隔中位约309秒 | 151升、155降、17平，中位变化0 |
| Go非流式，gpt-5.5，76 input token | 211 | 完成间隔中位约491秒，并非严格固定周期 | 100升、96降、15平，中位变化0 |
| Go、疑似填充型低输出请求 | 159 | 模型及周期混合 | 76升、77降、6平，中位变化0 |

同模型、同小时选取的普通成功事件粗略对照，5分钟业务量209升、207降、12平，中位变化同样为0。此对照未完全匹配事件前负载，只作描述性参考。

9月18日06:07–06:52的约45分钟业务空档中，仍有4次Go76和1次Kuma记录，业务没有随成功探测立即恢复。这是不能把“探针成功”直接等同“加并发”的明确反例。

## 哪些错误与减量有关

以下均比较事件前后15分钟，排除触发请求自身及窄口径疑似探针。状态码为错误记录分类。

| 北京时间 | 错误ID及类型 | 业务请求数前→后 | 估算平均在途前→后 |
|---|---|---:|---:|
| 9月16日19:03:25 | #234797，429，Too many pending requests | 537→148 | 13.91→3.48 |
| 9月17日20:47:42 | #270142，429，Too many pending requests | 112→3 | 2.41→0.22 |
| 9月17日19:55:19 | #268428，502，upstream connection error | 665→61 | 14.31→1.87 |
| 9月19日10:58:55 | #274244，503，overloaded | 141→64 | 3.77→2.72 |
| 9月19日19:33:45 | #274458，502，加密内容校验失败 | 125→59 | 4.54→1.67 |
| 9月16日17:32:40 | #230734，429，Too many pending requests（反例） | 264→233 | 5.52→5.43 |

- #270142后仍有少量请求；从20:47:58开始约74分钟没有业务新请求。此例的降流关联较强。
- #268428后约一分钟才进入约45分钟业务空档，不能说所有502都会触发相同处理。
- #274244后5分钟仍有57条业务、平均在途6.72；直到11:04以后才停。它不是立即停流样本。
- #274458之后仍有约9分钟业务，最终业务记录到19:43:28；不能说该密文错误直接导致停流。截至21:57尚未观察到后续请求，不对截止时间以后的流量作结论。
- #230734后总体业务基本保持，尽管同为排队类429。

为减少密集错误重复计数，对同一状态每至少30分钟抽取一个事件，比较前后15分钟：

| 记录类型 | 样本数 | 后/前请求率中位数 | 后/前平均在途中位数 | 请求率下降超过50%的样本 |
|---|---:|---:|---:|---:|
| 429 | 13 | 0.747 | 0.594 | 5 |
| 502 | 32 | 1.044 | 1.028 | 4 |
| 503 | 35 | 1.109 | 1.131 | 0 |
| 400 | 9 | 1.137 | 1.362 | 0 |

以上是探索性抽样，混合了不同负载、模型和相邻错误，不能把比值当作对方的降权公式。要求前后15分钟没有其他最终失败时，独立429仅3例：2次明显下降、1次基本不变，样本很小。

429共29条，文本上22条排队满、7条上游限流；归属上17条本地请求阶段、12条上游。不能把所有排队满都归于本站。503共1,167条，其中1,068条文本为overloaded，未观察到稳定的一报503就停流的规律。

## 可执行的核对方向，而非已确定算法

最值得向对方确认的是：其调配器是否把 `Too many pending requests` 视为渠道拥堵，并触发退避、降权或暂停；以及恢复/手工测试时是否连续测试多个模型。

本站只能看到分配到本站的流量，无法区分总体用户需求下降、切往其他供应商、客户端重试退避、管理员操作和自动调整配额。常规探针成功不是可靠的加量预测信号，503错误数量也不是可靠的降量预测信号。

当前配置复核时间2026-09-19 22:19：用户和Key均active，当前用户并发上限3000。此状态不能倒推历史配置，也不能排除上游账号等待队列限制。

## 归档与复查

本文和同目录的 `.summary.json` 已保存本次主要统计、精选事件与结论边界，后续引用不依赖临时文件。后续增加新的观察窗口时，应保留本快照，另建报告，并沿用相同的探针排除、时间重建和错误去重口径进行比较。

以下为调查时使用的临时工作底稿，仍位于仓库 `.tmp/`，可能随临时目录清理而失效，不属于本次持久归档附件：

- `.tmp/audit_codex_channel_series.json`：字段白名单脱敏时序，无凭证及请求正文。
- `.tmp/audit_codex_channel_analysis.json`：基本统计、小时趋势、空档。
- `.tmp/audit_codex_errors_analysis.json`：错误前后窗口、独立事件、精选案例。
- `.tmp/audit_codex_probe_analysis.json`：探测类别、周期与前后变化。

没有改动工作区既有业务修改，没有部署、迁移或生产写操作。
