# 系统测试

- 多账号归并到同一上游配置并按时间窗口筛选。
- 真实流量、主动探针和收益指标分栏展示，样本不足不伪造成功率。
- 新的三段式路由和导航可用，旧地址不再注册。
- 卡片详情抽屉可打开并跳转渠道、账号和使用记录。
- 无探针/无账号配置仍可返回；详情接口返回趋势、模型、错误、账号和收益可用性。
- 快速切换窗口或筛选不会被旧响应覆盖；抽屉支持 Enter、Space、Escape 和错误态展示。
- 看板跳转携带 `upstream_config_id` 后，使用记录、渠道和账号页面实际应用筛选。
