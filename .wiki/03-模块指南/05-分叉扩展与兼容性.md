---
title: 分叉扩展与兼容性
description: 相对 upstream/main 的长期业务合同、数据边界和维护规则
updated: 2026-09-22
owner: project
---

### 上游配置看板扩展

`upstream-config-dashboard` 是 fork 的管理面板扩展：保留真实流量与主动探针分离、配置级收益估算、账号构成和详情抽屉语义。看板聚合查询必须对左连接字段执行 NULL 安全处理，并按 `COALESCE(NULLIF(request_id, ''), id::text)` 去重错误记录。该扩展不新增数据库字段，不改变调度、利润控制、TTFT Guard 或可信度统计。

### 上游渠道余额告警扩展

`upstream-balance-channel-alert` 复用上游配置余额快照、人民币换算证据、`upstream_balance_low_threshold_cny` 和 `balance_low` 事件，为渠道列表及配置看板提供低余额提醒。余额不足只表示“新鲜且成功换算的 `balance_cny` 严格低于正阈值”；快照缺失/过期、解析或换算失败、非正阈值均必须显示余额不可用，不得误报为零余额，也不得读取账号 quota/credits 或站内用户余额。

低余额事件以配置为范围按周期幂等打开，首次跌破阈值通过默认开启的独立开关 `upstream_balance_notify_enabled` 发送一次通知；已配置的有效管理员通知邮箱优先，未配置时自动回退启用中管理员的合法注册邮箱。重复同步不重复发信，恢复关闭事件，再次跌破开启新周期。通知失败不能回滚快照/事件或阻断其它渠道同步。充值入口复用 provider 后台 URL 规则，邮件、事件和日志禁止写入凭据、认证头及原始响应。

### 用户余额不足自动提醒扩展

`user-balance-auto-notify` 在用户余额首次从有效阈值以上跌破阈值时发送邮件。存在已验证且未停用的自定义通知邮箱时，仅发送这些邮箱；没有有效自定义邮箱时自动回退用户注册邮箱。OAuth 历史账号的 `*.invalid` 合成占位邮箱不是可用收件人，界面要求先绑定真实邮箱或验证自定义邮箱。收件人仍经过统一 `EmailService.SendEmail`，因此 QQ 邮箱继承收件域名 SMTP 路由。

迁移 `254_enable_balance_notifications_for_existing_users.sql` 只在发布时将未软删除用户的提醒开关统一开启一次，并为受影响 API Key 写入认证缓存失效 outbox。发布 Gate 必须核对未删除用户转换数量、软删除用户摘要、VM outbox 增量和新版用户认证缓存触发器合同。发布后用户仍可关闭，系统不周期性强制重开。开关、阈值、注册邮箱及自定义邮箱变更必须失效用户 API Key 认证快照，避免计费路径继续使用旧收件规则。
# 分叉扩展与兼容性

本页是 Sub2API fork 相对 `upstream/main` 的长期知识入口。它记录稳定的业务合同、数据边界、运行约束和维护方法，不复制 `.spec/changes/**` 中的一次性 proposal、design、tasks 或测试报告。

## SSOT 与登记边界

- 代码、schema、迁移和测试是行为 SSOT。
- `.agents/skills/sub2api-fork-extension-audit/references/extensions.yaml` 是机器可读的 fork 差异登记 SSOT。
- 本页是面向维护者的长期解释和导航；登记项与本页必须能按扩展 ID 相互核对。
- `.spec/**`、`.wiki/**` 和 `.agents/skills/wiki-*` 是治理/知识资产，不作为 fork 业务扩展路径登记。
- `sub2api-fork-extension-audit` 用于只读审计，`sub2api-production-deploy` 用于发布、恢复和生产门禁；二者的 fork 专属运行合同仍需在本页和对应 skill 中保持一致。

后续流程：

```text
代码或迁移产生 fork 差异
        ↓
更新 extensions.yaml（机器事实）
        ↓
更新本页或对应模块 Wiki（长期解释）
        ↓
运行 audit + spec-wiki-lite validate --strict + diff check
        ↓
在 review/archive 后保留最终稳定合同
```

## 扩展总览

以下扩展与当前 catalog 的实际登记一一对应。路径和符号以 catalog 与源码为准，表格只保留长期维护所需的合同摘要。

| 扩展 ID | 领域 | 长期合同摘要 | 主要事实来源 |
| --- | --- | --- | --- |
| `upstream-config-management` | 上游入口 | 只拥有 upstream config、Key、聚合看板和账号入口；生命周期、模型同步、编辑白名单、余额告警和运行态策略由独立合同维护；派生身份/凭据只读，有效 Key 最多绑定一个未删除派生账号 | `backend/internal/service/upstream_config.go`、`upstream_dashboard.go`、`frontend/src/views/admin/UpstreamConfigsView.vue` |
| `user-balance-auto-notify` | 用户邮件 | 官方提供余额通知与 EmailService 基座；fork 增加有效自定义邮箱优先、注册邮箱回退、占位邮箱排除、迁移一次性开启现存用户，以及通知设置或邮箱变化后的认证快照失效 | `backend/internal/service/balance_notify_service.go`、`user_service.go`、`auth_email_binding.go`、`254_enable_balance_notifications_for_existing_users.sql` |
| `upstream-account-lifecycle` | 生命周期 | 仅完整同步快照推进缺失计数；`sync_managed` 达到连续缺失与宽限期后同事务软归档；manual 永不自动归档；恢复复用原账号 ID | `backend/internal/service/upstream_config.go`、`238_upstream_account_lifecycle.sql` |
| `upstream-model-capability-sync` | 模型能力 | 仅 `sync_managed` 自动写白名单；NewAPI 有效 `model_limits` 优先；30 分钟 freshness、24 小时 enforcement；失败不覆盖最近成功映射 | `backend/internal/service/upstream_model_sync.go`、`newapi_upstream_adapter.go` |
| `newapi-compatibility` | NewAPI | 兼容平铺/嵌套登录响应；当前会话注入 Bearer 与 `New-Api-User`；Token 不回写数据库 | `backend/internal/service/newapi_upstream_adapter.go` |
| `upstream-edit-policy` | 编辑白名单 | 上游派生账号的身份、Key、URL、代理、倍率、Priority 等受保护；局部运行字段按白名单编辑；OpenAI `openai_long_context_billing_enabled` 属于允许编辑的本地配置且仍做 boolean 校验 | `backend/internal/service/upstream_account_edit_policy.go`、`EditAccountModal.vue` |
| `shared-concurrency-loadfactor` | 并发调度 | 官方拥有普通账号并发准入、等待队列和 `LoadFactor` 基座；fork 只增加同一 `upstream_config_id` 的 Redis 槽位、lease、等待队列和负载率共享 | `backend/internal/service/*concurrency*.go`、`concurrency_cache.go` |
| `openai-capacity-failover` | 账号容量切换 | 后台“系统设置 → 网关设置 → 网关调度”可热更新；未保存后台覆盖时回落部署配置。普通 OpenAI 账号等待队列满按共享并发目标换号；所有上游绑定账号的 429 在同号重试耗尽后按物理账号换号；候选或独立预算耗尽使用可配置 HTTP 4xx/5xx，业务错误码固定为 `gateway_capacity_exhausted` | `backend/internal/service/gateway_capacity_failover_settings.go`、`backend/internal/handler/upstream_429_capacity_failover.go`、`frontend/src/views/admin/SettingsView.vue` |
| `openai-codex-state-tickets` | STATE 历史保护 | 保留 PR #7315/#7338 历史及旧私有字段的脱敏、导入剥离和普通编辑保留，不再运行内置票据服务 | `backend/internal/service/openai_codex_ticket_privacy.go`、`docs/CODEX_ACCOUNT_STATE.md` |
| `generic-plugin-runtime-v2` | 通用插件宿主 | 能力协商、受管范围准入、资源/动作、加密 CAS 与租约、维护升级和故障隔离；Manifest v1 iframe 保持兼容，v2 提供安全声明式单页面管理页；PostgreSQL 保存跨实例权威包，实例本地按需复验恢复；无票据业务常量 | `backend/internal/service/plugin_*.go`、`backend/pkg/pluginapi/`、[插件宿主与原生管理页](./11-插件宿主与原生管理页.md) |
| `codex-state-plugin` | Codex STATE 独立插件 | 三模型票据生命周期与 Native 单页独立更新，客户端优先、账号级代理组共享和完整响应 watchdog；正式双架构签名包可独立于宿主镜像发布，首次安装保持停用；旧 iframe 资源仅作回滚兼容 | `plugins/codex-state/`、[Codex STATE 插件](./08-Codex-STATE票据.md) |
| `openai-oauth-proxy-groups` | OAuth 代理组 | 参考 Go1c/sub2api PR #423 并按 0.2.7-baiyu 独立适配；一条 OpenAI OAuth/Setup Token 账号绑定一个代理组，账号总并发外叠加每代理槽，会话稳定绑定成员，Redis 故障和无出口均 fail-closed；STATE 保持账号加模型唯一票据槽 | `279_proxy_ip_groups.sql`、`proxy_ip_group.go`、`openai_proxy_group.go`、`ProxyIPGroupDialog.vue`、[OpenAI OAuth 账号代理组](./09-OpenAI-OAuth账号代理组.md) |
| `account-import-copy-proxies` | 导入协议拒绝残留 | deprecated/protocol-reject-only；管理端不再按代理复制账号，非空 `copy_proxy_ids` 明确返回 `COPY_PROXY_IMPORT_DEPRECATED`；旧共享指纹字段为惰性历史数据，不再参与身份派生，历史副本不自动合并或删除 | `account_data.go`、`ImportDataModal.vue`、[数据与任务](./04-数据与任务.md) |
| `fork-scheduling-compatibility-foundation` | 调度兼容底座 | 只拥有 `internal/forkscheduling` 的有界契约、纯 legacy 规则和 service bridge 解耦边界；TTFT、健康、并发、RPM、优先池和重试由各自运行时合同拥有 | `backend/internal/forkscheduling/**`、`backend/internal/service/fork_scheduling_bridge.go` |
| `gateway-request-group-mapping` | 网关定制 | 渠道定制首条命中规则可在认证后临时映射目标分组；永久 Key 绑定与 billing 元信息不变，后续准入、调度、计费、流水和 TTFT 使用目标分组 | `gateway_channel_customization_settings.go`、`gateway_customization.go`、`router.go`、`ChannelCustomizationView.vue` |
| `ttft-guard` | OpenAI 调度 | TTFT Guard 按 `group_id + account_id + canonical model` 隔离真实业务流量；分组使用 inherit/enabled/disabled 三态策略；仅携带实际 `group_id` 的业务首 Token 才采样，无分组账号测试和健康探针不进入分组 Guard | `backend/migrations/278_fork_group_ttft_guard_policies.sql`、`backend/internal/service/group_ttft_guard_policy.go`、`openai_ttft_guard*.go`、`openai_account_scheduler.go` |
| `group-profit-control-display` | 分组利润控制 | 官方拥有基础分组倍率和利润控制；fork 增加 `rate_percent` 真值、显式 0%、计费与配额分离，以及按真实调度口径计算的管理端账号倍率上限预览 | `277_user_group_rate_percent.sql`、`user_group_rate_repo.go`、`backend/internal/service/*profit_control.go`、`GroupRateMultipliersModal.vue` |
| `group-preferred-account-pool` | 分组优先账号池 | `account_groups.scheduler_preferred` 仅对当前最终分组生效；合法粘性和模型/能力/健康/利润/并发硬准入优先，随后优先池忽略倍率/成本排序并在无立即容量时回退普通池；列表点星与账号单改/批改共用该字段，显式提交时精确覆盖，旧调用保留未解绑关系星标 | `backend/internal/service/preferred_account_pool.go`、`admin_account.go`、`account_repo.go`、`GroupSelector.vue`、`GroupsView.vue`、`253_group_preferred_account_pool.sql` |
| `upstream-health-probes` | 健康观测 | OpenAI Responses、Anthropic Claude Code profile、Gemini 原生流式探针；首个非空文本定义首字；趋势最多 24 点、历史保留 35 天；请求级 HTML 403 与国内 Coding Plan 可恢复容量 403 不得升级为共享上游 Key 认证失败 | `backend/internal/service/upstream_health*.go`、`ratelimit_service.go`、`ratelimit_cn_providers.go`、`upstream_health*.go` repository |
| `admin-account-runtime-isolation` | 管理前端 | 普通账号页与上游管理页隔离列、排序、刷新、ETag、请求代际和静默窗口；健康历史共享 Tooltip；TTFT ticker 全页单例；图片成本结构化展示 | `frontend/src/views/admin/AccountsView.vue`、相关 account/common 组件 |
| `probe-guard` | 探针保护 | 默认错误分类与自定义错误码追加合并；429/529 默认降级；probe-owned 临时不可调度与恢复阈值独立于业务来源状态 | `backend/internal/service/upstream_probe_guard*.go`、管理设置组件 |
| `upstream-cost-attribution` | 成本归因 | 官方拥有基础账号 usage、余额和倍率数据；fork 增加 upstream config、实际倍率、Priority、价格、usage 与 batch-image 的不可漂移快照归因 | `backend/internal/service/*rate*.go`、`187/195` migrations |
| `channel-monitor-v2` | 渠道监控 | 官方拥有基础监控 CRUD 与主动探测框架；fork 增加 managed API Key、倍率趋势、分组可见性、权限、隐私默认值、错误分类、V1 主动 SSE/TTFT、后台取消竞态和全局成功降级策略 | `backend/internal/service/channel_monitor*.go`、`backend/internal/repository/channel_monitor*.go`、`backend/cmd/server/main.go`、`backend/cmd/server/shutdown.go`、`216/217/218/252` migrations |
| `quality-and-usage-aggregation` | 质量/用量 | 官方拥有 usage log 与基础用量查询；fork 增加账号/分组 1H/24H 质量遥测、缓存率评分、首字后生成 TPS、hourly/daily 聚合 coverage/backfill 和 raw usage 清理边界 | `account_quality.go`、`usage_log.go`、`usage_log_repo_stats.go`、`UsageTable.vue`、`UsageView.vue`、`209/235/236` migrations |
| `image-cost-routing-and-display` | 图片/视频能力 | 图片能力只来自 Key 快照；未知成本不当作 0；仅显式启用成本路由的分组参与成本分层；视频运行时只支持 Grok，管理页徽标还必须有有效 effective video multiplier 和至少一个分辨率价格，不能只凭 `allow_video_generation` 判定支持 | `upstream_key_image_pricing.go`、`openai_images.go`、`AccountsView.vue`、`upstreamVideoCapability.ts`、`237_image_cost_routing.sql` |
| `migration-profile-contract` | 迁移/版本 | 历史 profile 不回写；fork VERSION 为官方 VERSION 加 `-baiyu`；每次 fork VERSION 变化都新增下一个连续 profile，即使无 migration 也使用 `new_migrations=[]`；audit checksum 绑定原始字节，release manifest checksum 绑定 strip 后 SQL，二者不可混用 | `backend/migrations/**`、`production-deploy/scripts/release/**` |
| `release-operations-isolation` | 发布运维 | fork 发布资产隔离在 production-deploy skill；发布使用 `fast/specialized/full` 三层门禁，时间预算分别为 0–2、5–15、20–60 分钟；`full` 不阻塞普通发布，但恢复链自身变化时为硬门禁；恢复 helper 原子版本、幂等 checkpoint、候选验真与恢复验真彼此独立；RackNerd 控制与产物传输固定经 DMIT SSH + 1080 HTTP CONNECT | `.agents/skills/sub2api-production-deploy/**`、[发布与协调恢复](./10-发布与协调恢复.md) |
| `prompt-security-audit` | 安全审计 | 运行时采用 upstream；fork 仅适配官方 migration `181/182` 到本地 `193/194` 并保留集成回归，发布日志不得写入 prompt 原文 | `backend/internal/securityaudit/**`、`193/194` migrations |
| `upstream-observation-preference` | 观测偏好 | 管理员观测开关持久化；关闭观测仍可手动探测但不执行定时探针，同步、重启、归档与恢复不得擅自重开 | `upstream_health.go`、`upstream_config.go`、`240_upstream_observation_preference.sql` |
| `upstream-precise-effective-rate` | 精确倍率 | `source_rate × recharge_rate` 的十位小数定点值是 Key、派生账号、图片成本、usage 快照和同 Priority 排序的权威实际倍率 | `upstream_config.go`、`openai_account_scheduler.go`、`241_precise_upstream_effective_rate.sql` |
| `upstream-model-alias-sync` | 模型别名 | 全局 source→target 规则只作用于 `sync_managed`；自动 identity/alias 与手工映射合并；同步失败保留旧 mapping；设置保存不立即全量同步 | `upstream_model_alias_rules.go`、`upstream_model_sync.go` |
| `upstream-global-retry-status-codes` | 上游重试 | `upstream_pool_mode_retry_status_codes` 是上游账号唯一的同账号重试状态码来源；设置保存只更新系统设置，不再回写账号凭据；新建上游账号自动继承，空值恢复 `401/403/429`；普通账号保留原有账号级语义 | `pool_mode_retry_settings.go`、`account.go`、`upstream_config.go`、`UpstreamManagementSettingsDialog.vue` |
| `upstream-auth-session-and-key-url` | 认证/URL/协议 | provider-neutral 认证会话按 config 唯一且删除同事务清理；密文只加密存储，不进 API/日志；上游绑定账号始终以真实 Key `base_url` 为协议探针和业务请求地址，adaptive 仅在能力确认后走原生 Responses；账号级可覆盖 `api_protocol`，但不可改写绑定地址和密钥 | `account.go`、`upstream_account_edit_policy.go`、`upstream_health_probe_client.go`、`EditAccountModal.vue`、`246/247` migrations |
| `upstream-confidence-juice-probe` | Juice 可信度 | `openai-juice-multiprobe-v2` 单请求多探针：70% Juice、15% Coverage、15% 输出完整性；Juice 固定使用 high effort（Sol/Terra/Luna 分别为 40/32/48，避免其它档位的重合指纹）；网络异常不进 Juice 分母，Coverage/输出改写单独作为 24h 硬异常；保存 expected/observed 结构化证据并在 tooltip 展示；显式启用配置才独立调度和反馈；非 OpenAI 不显示可信度占位 | `upstream_confidence_probe_settings.go`、`upstream_health_probe_client.go`、`upstream_health_observation_repo.go`、`UpstreamHealthCell.vue`、`248/249/250` migrations |
| `registration-email-abuse-guard` | 注册反滥用 | 官方拥有注册、验证码和 OAuth 建号主链；fork 增加收件箱身份去重，以及 Gmail 家族在显式白名单下的普通地址限制；已有账号登录、QQ 和其它域名不受新增规则影响 | `registration_email_alias.go`、`registration_email_policy.go`、`RegisterView.vue`、`EmailVerifyView.vue` |
| `user-recharge-store` | 用户支付入口 | 固定“充值商城”与原生购买入口并存；独立路由原样加载 catfk 商城，不向第三方传 Token、用户 ID或来源上下文；CSP 只放行精确 origin | `RechargeStoreView.vue`、`AppSidebar.vue`、`router/index.ts`、`setting_public.go` |
| `daily-activity-rewards` | 用户活动 | 每日礼包、充值/消费抽奖和独立邀请达标抽奖使用后端规则、机会和奖励账本；充值来源与邀请返利资格口径一致，管理员正数 add 仅在既有开关开启时计入；充值与消费按上海活动日分别取整后永久累计，已消费机会仍计入已发总数；邀请对象成功累计充值达到 10 元只计一次，每 5 次产生永久机会；目标日补发必须先生成绑定明细的计划校验值，再在事务锁内应用并二次核验；活动奖励不写入 AffiliateService 返利账本 | `backend/internal/service/daily_activity_service.go`、`backend/cmd/daily-activity-backfill/main.go`、`frontend/src/views/user/DailyActivitiesView.vue`、`260/261` migrations |
| `astrbot-channel-status-plugin` | 渠道状态机器人 | 独立 AstrBot 插件用统一事件 API 拉取渠道快照并渲染一张完整状态图；优先 V2 snapshot，失败回退 V1 monitor history；/status 与定时推送均发送一张图；按平台分组展示模型、延迟、可用性、分组和普通倍率；不泄露管理员 API Key，不改变网关计费或调度 | `astrbot/main.py`、`astrbot/client.py`、`astrbot/renderer.py`、`astrbot/tests/test_plugin.py` |

### 请求级分组映射契约

`gateway-request-group-mapping` 扩展渠道定制的既有首条命中规则，新增 `group_mapping` 动作。渠道定制启用时，网关把认证拆为“Key/User/IP 身份认证”和“最终分组准入”：映射发生在两者之间，并早于分组模型白名单与 Composite 路由解析。运行时克隆当前请求的 API Key，替换克隆对象的 `GroupID`、`Group` 和请求分组上下文，数据库中的永久绑定及原 API Key 对象保持不变。最终分组统一承担分组状态、用户授权、Key 配额、余额或订阅限额校验；后续账号选择、TTFT Guard、利润保护、专属/高峰倍率、实际扣费和 usage 流水也读取该分组。`GET /v1/sub2api/billing`、倍率探针和永久 Key 元信息继续描述原分组。

目标分组必须由可信仓储完整加载、处于启用状态并允许当前用户绑定。订阅目标还必须存在有效订阅，通过限额预检，并在窗口到期时同步维护后使用回读快照；映射到标准分组时必须清除原订阅上下文。Gemini、Antigravity 与强制平台入口只接受兼容平台或 Composite。任何目标失效、无权限、订阅不可用、限额超出或入口不兼容都必须明确拒绝，禁止静默回退原分组。

旧规则缺少 `action` 时按 `local_response` 归一，规则仍按页面顺序首条命中，不能叠加本地响应和分组映射。消息精确匹配只裁剪配置文本和请求文本的首尾空白，读取请求体后必须重放给下游。两种动作的命中统计每个请求最多增加一次；该能力不新增 schema、migration 或持久化分组覆盖字段。

### 分组优先账号编辑契约

账号单改和批改复用 `account_groups.scheduler_preferred`，不新增账号字段。账号列表和单账号详情都必须返回由该关系投影出的 `preferred_group_ids`；编辑端在兼容旧详情响应时可从 `account_groups[].scheduler_preferred` 回退推导，避免编辑其他字段时误清空已有星标。前端只有在分组已勾选时才允许点星，取消分组必须同步移除该星标；`preferred_group_ids` 始终是同请求 `group_ids` 的子集。

显式同时提交 `group_ids` 与 `preferred_group_ids` 时，后端在一个关系事务内精确替换分组顺序与星标集合，并只写入一次 `account_groups_changed` 调度刷新事件。批量编辑采用覆盖语义：只有启用“修改分组”才提交这两个字段，空星标集合表示清除所有最终分组的优先状态，筛选目标与手工选择目标行为一致。

兼容调用未提交 `preferred_group_ids` 时继续走原 `BindGroups` 语义：删除分组会删除对应星标，保留的关系继续保留原星标，新关系默认不优先。该管理能力通过独立的 `AccountGroupPreferenceRepository` 窄接口接入，不扩大网关和只读调度仓储的依赖面。

## 关键运行合同

### Codex STATE 票据

`openai-codex-state-tickets` 现在仅保留历史与私有字段保护。运行逻辑迁入 `plugins/codex-state/`，由 `codex-state-plugin` 独立维护；宿主通用契约登记为 `generic-plugin-runtime-v2`。不迁移旧配置和票据，新插件默认关闭。#7315 head/merge 固定为 `3c2f05c957b4b93866318ec8695fc5a28fff70eb` / `49a39b6dc1abed30fd227611e8af1108bc427610`，由 #7338 父历史带入而不重复合并；#7338 head 固定为 `09e112fb4997be666b10da48c3b5f65fb8d8b53b`，fork merge 固定为 `a6be2645fffca32a2425d79f822902d7a5aedaff`，不得在后续 upstream 重核中丢失作者和父历史。

设计参考固定为 `ccodex-sleep-state@26b22196bf68b372d0daad9381f686a3321068d4` 与 `@b18fabf9ad8e9d7af7d9d0306b623ba6091a39d6`。前者是 #7338 原参考点，后者是 2026-09-19 本轮设计复核点。参考项目为 GPL-3.0，本项目为 LGPL-3.0-or-later，因此只允许借鉴公开状态机和验证思路，必须在本项目边界内独立实现，不得复制整项目源码、具体函数实现或引入其依赖。明确不采用其本地 Codex 配置接管、CCS/profile/Web 面板、订阅与代理节点池、纯内存 STATE、业务响应正文拦截、`on_demand/standby` 用户策略、本地 relay、原生上游 WebSocket、账号暂停恢复或配置恢复行为。

运行态按 `account_id + canonical outbound model + config revision + ChatGPT identity` 隔离，Astra、Sol、Terra 不共享票据、异常计数或冷却。代理 ID、URL、代理组和成员变化不进入票据所有权；代理组通过任一可用代表业务出口复验后，全部成员共用账号级 active/ready。客户端同账号 STATE 保留，已知跨账号 STATE 剥离，无可用客户端 STATE 时才注入后台 active；显式启用模型缺票时 strict 只排除该账号/模型组合，TTFT fail-open 不得绕过。原生上游 WebSocket 暂不属于支持范围，HTTP bridge 测试不得作为原生 WS 支持证据。完整来源、状态机、数据和回归边界见 [Codex STATE 票据](./08-Codex-STATE票据.md)。

### OpenAI OAuth 代理组与导入兼容

`openai-oauth-proxy-groups` 的设计参考是 Go1c/sub2api PR #423，采用方式固定为当前 `0.2.7-baiyu` 独立适配，不复制其分支实现，也不把开放 PR 当作 upstream 正式事实。账号只能在单代理与代理组之间二选一；代理组只对 OpenAI OAuth/Setup Token 开放。账号总并发仍是全组总闸，成员另外受 `account_id + proxy_id` 槽限制；有可靠 SessionHash 时稳定绑定成员，已有绑定未满不迁移，新会话按实时成员并发最少优先并对同负载成员打散，绑定成员满载时才重绑。Redis 绑定或成员槽不可用时 fail-closed，禁止绕过限制或意外直连。

STATE 保持账号加模型唯一槽，不因代理、组或成员变化重建。动态代理采集后的候选由组内可用代表依次复验，任一成功即可发布给整组；只有 ChatGPT 身份变化才强制废弃旧票。导入端每个源账号只创建一条记录并可选择代理组；旧 `copy_proxy_ids` 仅保留稳定拒绝，旧共享指纹字段不再参与身份派生，历史副本不自动归并、停用或删除。完整合同见 [OpenAI OAuth 账号代理组](./09-OpenAI-OAuth账号代理组.md)。

### 账号容量切换与上游 429

容量切换运行时设置保存在 `gateway_capacity_failover_settings`，后台入口为“系统设置 → 网关设置 → 网关调度”。数据库设置优先于 `gateway.scheduling.capacity_failover_*` 部署配置；未保存数据库覆盖或数据库不可用且没有可用快照时，回落部署配置。默认仍为关闭、最多换号 10 次、耗尽返回 503。执行保存的实例立即发布进程快照，其他实例通过 60 秒缓存 TTL 收敛；请求热路径只读快照，不直接查询数据库。

开启后存在两类独立触发源。普通 OpenAI 短连接 HTTP 请求在所选账号等待队列已满、尚未向客户端输出内容时，将该账号及其 `SchedulingConcurrencyTarget` 加入当前请求的排除集合并重新选号；相同 `upstream_config_id` 的派生账号共享并发目标，确认满载后不得重复尝试同一池。所有平台的上游绑定账号实际请求返回 429 时，先执行既有池模式同账号重试；同号预算耗尽后按物理账号排除并重新选号。

容量切换使用独立的 `max_switches` 预算，合法范围 0–1000；默认 10，填 0 表示不限次数，依靠当前请求排除集合与既有 `max_account_switches` 安全上限终止。普通账号按共享并发目标计数，上游 429 按实际账号切换计数；同账号重试不消费该预算。设置关闭时不改变既有上游重试和换号，只保留最终 429。用户/API Key RPM、本地并发、利润否决、尚未发起上游请求的本地错误及普通非上游账号的供应商 429 均不进入该策略。

上游 429 的容量耗尽判定优先于错误透传规则。最终响应按各入口协议生成 `gateway_capacity_exhausted`；Ops 同时保留真实 `upstream_status=429` 与客户端状态。有效 `Retry-After` 在本次尝试中取最短值，缺失时使用 5 秒，换号成功不得遗留该响应头。流式已输出语义内容时禁止换号和拼流，只发送协议合法终止事件；Responses WebSocket 使用 1013 关闭并记录配置的 intended status。

所有可用候选都因账号容量不足耗尽时，HTTP 状态由 `capacity_failover_exhausted_status_code` 决定，合法范围为 400–599、默认 503；JSON `error.type` 固定为 `api_error`，`error.code` 固定为 `gateway_capacity_exhausted`。该机制不修改账号全局健康、`schedulable`、OAuth 状态或临时封禁，只记录请求级排除、结构化事件和进程内累计指标。

### Fork 调度扩展兼容底座

`backend/internal/forkscheduling` 是 fork 专属的窄契约和纯规则层，只接受有界值对象、只读快照和小型能力接口，不依赖官方 `service`、repository、Ent、Gin、Redis 或执行闭包。`backend/internal/service/fork_scheduling_bridge.go` 是唯一适配边界：它把官方账号、健康、TTFT、并发、RPM、优先池和重试状态映射到契约，再把结果转换回官方调用链。

官方 service 仍拥有请求 selector、handler、failover、槽位/lease、健康持久化、计费和 usage log 的执行权。TTFT 与健康 runtime 在同一 service 实例内复用既有状态；没有 service owner 的健康 runtime 只能读取，不能直接写全局 Registry。未注入可选 fork 能力时保持原 fallback，不新增后台 goroutine、文件、网络、数据库字段、migration 或 Redis key/lease 格式。候选和快照不得携带 API Key、Cookie、Token、请求体、响应体或完整凭据。

分组 TTFT Guard 策略由 fork 独立表 `fork_group_ttft_guard_policies` 持久化，不向官方 `groups` 表增加字段。策略写入与 `group_changed` scheduler outbox 在同一事务；幂等写入不发事件。每个实例收到 `group_changed` 后先清策略缓存，再清该分组本实例 TTFT 运行态。`inherit` 缓存命中时仍读取 `SettingService.OpenAITTFTGuardConfigSnapshot()` 热快照更新全局开关、阈值和样本数，自定义 `enabled` 与 `disabled` 策略不受全局变化影响。全局 TTFT 配置成功写入后通过 fork 独立 Redis 失效通道发布 `global` 事件；远端实例必须重新读取持久化配置，只有读取成功时才清理配置已变化的 `source=global` 运行态，读取失败必须保留最后有效快照和现有状态。Redis 即时通知失败只记录告警，不得把已经提交的设置报告为保存失败；远端实例通过全局配置热快照 TTL 最终收敛。策略缓存使用 generation 防止失效期间开始的旧数据库读取重新回填缓存。只有从 API Key 和实际路由上下文取得正数 `group_id` 的真实业务首 Token 才能写入分组运行态；无分组上下文的后台账号测试、健康探针和其他主动探针既不采样，也不得创建、恢复或延长任何分组 TTFT Guard 状态。运行时 degradation 必须携带 `group_id`、`group_name` 和策略来源。

### 探针、真实流量与调度反馈

可信度配置状态必须区分“缺失”和“显式保存”。只有数据库中存在、JSON 合法且 `enabled=true` 时，OpenAI 才进入 Juice 独立模式：后台探针只看 `LastProbeAt` freshness，不被 `LastEvidenceAt` 抑制；探针收到首字时可将 TTFT 反馈给 OpenAI 调度器，并按原始网络请求结果上报成功/失败。

配置缺失、空值、disabled、读取失败或非法 JSON 统一视为未启用。此时普通后台健康探针沿用真实流量抑制：`max(LastProbeAt, LastEvidenceAt)` 在 freshness 窗口内则跳过。管理员手动探针绕过该抑制。非 OpenAI 可以复用未来的调度反馈接口，但当前不显示可信度徽标，也不使用 OpenAI 专属质量判断。

探针请求的网络结果必须先于 Juice 质量降级保存：

- HTTP/传输错误、超时、取消、流中断、无有效输出：无 TTFT，失败率样本；可信度分母不增加。
- 完整收到响应但 Juice 值错误或 `mixed`：有 TTFT，网络成功率样本；只降低可信度，不改写网络 success。
- `current_success`：有效完成成功样本。
- `unsuccessful`：有返回但答案不正确的有效完成失败样本。

可信度 24h/7d 分数为：

```text
current_success / valid_completed × 100
```

样本不足必须单独标记，不能伪装成 0 分。可信度旧版本（v1）数据不参与新版本统计；清理只清空旧可信度字段，保留同一健康观测中的普通状态、HTTP、TTFT 和时间证据。

### 质量列缓存率

上游管理和账号/分组列表的质量列在 1H、24H 行展示缓存率，位置位于首字/总耗时之后、样本数之前。缓存率只读取成功的真实流式 `usage_logs`（`actual_cost > 0`、`stream = TRUE`），健康探针和 Juice 探针不进入该统计。后端同时返回缓存读取 token（分子）与提示词总量（输入 + 缓存创建 + 缓存读取，分母），前端在 tooltip 中展示两者；分母为 0 时 `cache_rate` 为空，不显示伪造的 0%。

质量分版本为 5：当首字、总耗时和缓存率均有效时使用 50%/15%/35% 权重；缺少缓存率时保持旧版首字/总耗时 85%/15% 语义；仅部分指标有效时按可用权重归一化。版本 5 将缓存创建 Token 纳入缓存率分母，与 Token 使用趋势和 Channel Monitor 的口径一致。质量分仍是只读展示指标，不参与账号调度或计费。

### 渠道监控与主动探针流式化

渠道监控 V1 的 OpenAI Chat/Responses、Grok、Kimi、智谱、DeepSeek、Anthropic 和 Gemini 主动探针默认使用 SSE 流式请求。首字只认首个非空文本增量；`ttft_ms` 保存首字耗时，`latency_ms` 继续保存完整请求耗时。收到首字后断流仍保留 TTFT 但记录失败；空流、HTTP/传输错误和请求自身超时按失败记录。调度器取消（Stop、Unschedule、Schedule 替换）会取消已入队或执行中的请求，不写历史也不推进 `last_checked_at`；管理员手动 `RunCheck` 不受该抑制影响。

`replace` body 明确设置 `stream=false` 时保持非流式 JSON 兼容；缺省 adapter 请求不回退为整包读取。渠道监控历史的 `ttft_ms` 为 nullable，旧历史保持 NULL，不用总耗时回填；V2 被动业务流量聚合和可用率口径不变。上游账号主动探针复用同一增量语义：Grok 使用 Chat SSE，Antigravity 仅主动测试路径使用流式解析，普通业务路径不改变；真实 provider 流式能力在发布验证中标记为 `not_checked`，由隔离 SSE fixture 和代码测试提供证据。

TTFT 的计时起点是每次主动请求调用 HTTP 客户端前的请求开始时间，直到首个非空、非 reasoning、非协议文本增量；响应头前的连接、TLS、排队和等待均计入。首字耗时不足 1ms 时保存为 1ms，未收到可见文本保持 NULL；首字后断流保留 TTFT 但仍记录失败。读取 history、latest、timeline 和立即探测 API 时，旧数据中的 `ttft_ms <= 0` 统一归一为 NULL，不更新历史行。`latency_ms` 始终表示整轮请求（含重试等待）的总耗时，V1 降级只读取总耗时、重试和换号阈值，TTFT 只用于展示。

批量时间线 SQL 的稳定列顺序为 `monitor_id, status, latency_ms, ttft_ms, ping_latency_ms, checked_at`；repository 与 Scan 必须保持同序，确保 `operational/degraded/error` 状态正确到达前端。前端对 NULL/0 TTFT 显示 `-`，总耗时继续单独显示，时间线颜色仍由 status 映射绿/黄/红。

V1 的成功探测降级策略由三个 settings 实时控制：总耗时阈值（默认 6 秒，1–300）、免降级额外重试次数（默认 2，0–4）和免降级累计换号次数（默认 3，0–5）。首次请求不计入重试；重试与换号分别判断，不互相抵扣。最终成功只要任一阈值超限即为 degraded，并保留具体原因；失败、超时、流中断和 challenge 错误仍为失败状态。单监控 `max_probe_attempts` 仍只限制一次探测实际请求上限。

### 管理员用量明细生成 TPS

管理员“使用记录 → 用量明细”的延迟列在首字和总耗时之后展示生成 TPS，Excel 导出同步包含“生成 TPS”。该值表示首个 Token 返回后的文本生成速度，而非包含排队与首字等待的端到端吞吐：

```text
output_tps = output_tokens × 1000 / (duration_ms - first_token_ms)
```

只有 `output_tokens > 0`、`first_token_ms` 存在且 `duration_ms > first_token_ms` 时才返回派生字段 `output_tps`；无首字、无输出 Token、耗时异常以及图片/视频等非文本生成记录均返回空值，前端显示 `-`，不伪造 `0 TPS`。管理员延迟单元格以“首字 / 总耗时 / 输出速率”三行紧凑展示，输出速率保留一位小数并使用 `t/s` 单位；Excel 导出仍直接使用 API 派生值和既有“生成 TPS”字段，不在前端重复计算。

TPS 计算器由用量明细和健康探针共享，避免口径漂移。该字段不持久化、不需要数据库迁移、不支持服务端排序，且不扩展到普通用户用量页。合同回归覆盖 `backend/internal/service/usage_log_test.go`、`backend/internal/handler/dto/mappers_usage_test.go`、`backend/internal/service/upstream_health_probe_client_test.go`、`frontend/src/components/admin/usage/__tests__/UsageTable.spec.ts` 和 `frontend/src/views/admin/__tests__/UsageView.spec.ts`。

### 注册邮箱与 Gmail 普通地址

注册链路保留收件箱身份去重：所有域名去除 `+suffix` 参与查重，Gmail 家族额外去除本地部分点号，并将 `googlemail.com` 折叠为 `gmail.com`。该规则用于防止同一收件箱以不同字面地址重复注册，并由仓储锁与复查覆盖并发创建。

当归一化后的邮箱域名白名单显式包含 `@gmail.com` 或 `@googlemail.com` 时，额外启用 Gmail 普通地址限制：新注册 Gmail 家族本地部分必须匹配 `^[a-z0-9]+$`（大小写归一后判断）。点号变体、`+tag`、下划线、连字符和其它符号在发送注册验证码、普通注册及 OAuth 首次建号前返回 `GMAIL_ALIAS_NOT_ALLOWED`；错误不得包含用户邮箱。白名单为空或不含 Gmail 家族时不启用该额外限制，QQ 与其它域名保持现有规则。

该限制只针对新账号创建，不影响已有账号的密码/OAuth 登录，也不扩展到邮箱绑定。前端注册页与邮箱验证页只做同口径提前提示，后端始终是权威校验。不维护临时邮箱域名黑名单，不调用第三方邮箱信誉服务，不新增字段或 migration。

### 能力、倍率与路由

- 用户专属倍率以 `user_group_rate_multipliers.rate_percent` 作为唯一业务真值，有效倍率固定为“分组普通倍率 × 专属百分比 ÷ 100”。修改普通倍率只改变当前有效倍率和兼容影子列，不得改写已保存百分比；运行时缓存百分比并结合请求使用的当前普通倍率动态计算，避免普通倍率更新后继续命中旧绝对值。
- 旧 `rate_multiplier` 列继续作为兼容影子值：新接口优先读写 `rate_percent`，旧客户端提交绝对倍率时按当前普通倍率反算百分比，两者同时提交必须拒绝。显式 `0%` 与 `NULL` 严格区分，`NULL` 才表示清除专属配置并回退普通倍率；倍率同步在事务内替换，清空倍率不得删除同一关系上的 RPM override。
- 分组利润控制的下游有效倍率为“用户专属倍率（存在时）否则分组默认倍率”，订阅高峰窗口内再乘当前高峰因子；管理端分组列表、编辑弹窗和专属倍率表共用同一预览公式，结果向下保留四位且不参与保存。
- 用户专属倍率允许显式 `0`，而分组默认倍率仍必须大于 `0`。`0` 只对当前用户-计费分组免除余额/订阅消费和利润控制；Token、请求数、`total_cost`、原始成本、账号成本及统计照常记录，`actual_cost/user_spend` 保持为 `0`。API Key 配额、时间窗限速和用户×平台配额使用按分组原价计算的 `quota_meter_cost`，非金额限制和其它用户/分组利润门不变；`nil` 仍表示回退分组默认倍率。
- 图片能力、图片成本和视频成本均以 Key 快照及 effective multiplier 为依据，未知/过期/部分数据不得显示为免费。
- `video_rate_independent=true` 时，必须存在合法 `video_rate_multiplier`；缺失或无效时 `supported=false`。
- `prefer_lowest`/`strict_lowest` 只能在健康、共享并发、TTFT Guard 和 Priority 约束通过后进行成本排序。
- 精确 effective rate 贯穿 Key、派生账号、图片成本、scheduler metadata、调度日志和未来 usage 快照；历史 usage 不回算。

### 用户活动与独立邀请抽奖

每日活动是 fork 的独立奖励扩展，不复用外部活动页面接口、脚本或 token，也不把活动奖励混入现有邀请返利。充值成功结算、真实消费结算和邀请达标事件先按活动日/事件去重；失败、取消、退款和管理员赠送不进入充值门槛，显式用户专属倍率 0 的消费不增加消费抽奖进度。

每日礼包按 `Asia/Shanghai` 活动日每用户最多一次；充值/消费进度每日重置，抽奖机会、邀请达标计数和奖励历史永久保留。被邀请用户累计成功充值达到 10 元时，按 `(inviter_id, invitee_id)` 只产生一次达标计数；每 5 个达标邀请生成一个永久邀请抽奖机会，奖励区间为 5～10 元。充值抽奖和消费抽奖默认每满 50 元产生机会，默认奖励区间为 0.50～1.00 元。

用户接口只接受活动类型、数量和幂等键，身份来自认证上下文；门槛、可用机会、奖励金额、随机结果和规则版本由后端计算。抽奖事务锁定机会、生成整数分/Decimal 奖励、写入独立活动账本并通过统一余额服务入账，唯一约束与幂等键防止重复发放。管理员配置受后端硬上限和审计约束，历史奖励保留生效规则版本，不因后续配置变化重算。

### 数据与迁移

人工额外成本使用 `extra_cost_entries` 追加式流水，日期按 `Asia/Shanghai` 归属，金额计入管理员账号成本汇总与成本趋势，但不写入 `usage_logs`，不改变用户扣费、配额、利润控制及用户/分组/渠道统计。原记录不可编辑或删除，纠错只能追加等额冲正记录；新增和冲正后必须清理仪表盘、趋势与用量统计缓存。迁移为 `262_extra_cost_entries.sql`，字段和幂等键保持可审计。

额外成本弹窗的原生数字输入通过 Vue `v-model` 返回数字或空字符串；金额状态和校验必须兼容两种类型，禁止直接对金额调用字符串方法。空值不可提交，`0` 是有效金额；输入本身不创建流水，提交时仍发送数值金额。输入回归使用 `ExtraCostsDialog.spec.ts` 覆盖整数、小数、零、清空和非法值。

管理员成本摘要通过 `AccountCostAmount.vue` 仅显示最终合计，悬浮、点击或键盘聚焦时显示“用量账号成本 + 额外成本”，保持原金额精度、颜色和服务端合计优先规则。用量页总消费中的成本保留“成本：”前缀；仪表盘今日和累计两处不显示该前缀。明细收起时不占布局，不改变费用计算或请求。

迁移长期合同包括 upstream 绑定唯一性、账号生命周期、图片成本路由、观测偏好、精确倍率、认证会话、Key URL、Juice 配置、旧可信度清理、Key 平台目录、用户专属倍率百分比、分组 TTFT Guard 策略和 OAuth 代理组。迁移清单、profile、checksum 和 compatibility identity 以 audit catalog 与 release profile 为准；历史 profile 不回写。profile 253 保留 migration 277 的历史合同；当前 pending profile 254 的 parent 为 253，版本为 `0.2.7-baiyu`，`new_migrations` 精确为 `278_fork_group_ttft_guard_policies.sql`、`279_proxy_ip_groups.sql`。两项 Git 原始文件 SHA-256 必须与 `extensions.yaml.migration_contracts` 登记一致；migration 279 固定每代理并发默认 `10`、范围 `1–1000`，并保持单代理/代理组互斥。migration 276 作为已发布历史兼容列保留原字节和 checksum，当前运行时、API 和前端不再读取或配置 `scheduling_mode`。

任何涉及密文、Token、Cookie、认证头、URL、上游响应正文或 prompt 原文的日志/审计输出，都必须遵循脱敏和 allowlist 边界。

### 未正式发布的 upstream 批次

fork 可以采用 upstream `main` 中尚未进入正式 tag 的提交，但必须在扩展目录登记完整 base、tip、真实 merge commit、提交数和采用时版本。2026-09-18 采用的批次从 `881f3202694c6bc932446931a30c27d9675178b9` 到 `efe9aab1e4ec89a42ba45e8dac20e882c5409a6a`，由 merge commit `f501d6ee9461b86077a376002f0048d94e16cd61` 引入，共 52 个提交，其中 27 个为非 merge 源提交。该批次采用时官方 `VERSION` 仍为 `0.2.5`，因此 fork 保持 `0.2.5-baiyu`，不提前使用 `0.2.6`。

upstream 后续回退、force-push、撤回 PR 或删除远端引用时，不删除 fork 已采用的提交，不重写 merge 历史，也不因远端不可达自动回滚业务实现。官方最终跳过 `0.2.6` 并发布 `v0.2.7`；目标提交 `aea725f2ea644d5592d0bbb1d63b607efa7e200a` 包含该批次全部 27 个非 merge 源提交，range-diff 全部相同，且批次触及路径在目标树中没有后续漂移。因此这些提交全部归回 upstream 维护归属，没有 fork 保留项或部分覆盖项；原 merge commit 和 46 个 recovery 引用继续保留，不删除历史。

官方 `v0.2.7` 的 annotated tag 发布流程在构建阶段把 `VERSION` 写为 `0.2.7`，标签源码中的文件仍为 `0.2.5`。该异常使用目标 commit 到 release version 的显式目录绑定解决：fork 版本为 `0.2.7-baiyu`，不能继续显示 `0.2.5-baiyu`，也不能仅凭未来 tag 名猜测版本。

归属重核不会删除既有 Git 历史。“剔除已进入正式版的提交”仅表示后续冲突处理中不再平行维护重复实现，并把维护归属改为 upstream。正式版未覆盖或仅部分覆盖的行为必须绑定真实存在且覆盖对应路径的扩展 ID、路径、不变量和最低测试，继续由 fork 维护；正式目标版本到达前不得提前把重核标记为完成。

## 后续维护规则

每次 fork 新增或修改与 `upstream/main` 不同的行为时，必须完成以下闭环：

1. 在 `extensions.yaml` 新增或更新唯一扩展 ID、路径、符号、迁移、测试和 invariants。
2. 在本页对应章节补充长期合同；若已有模块页覆盖，则更新模块页并在本页总览中保留入口。
3. 不登记 `.spec/**`、`.wiki/**` 或 `.agents/skills/wiki-*`；这些是治理和知识资产。
4. 运行 fork audit、`spec-wiki-lite validate --strict`、相关测试和 `git diff --check`。
5. 只有实现完成并通过 review/archive 后，才把仍有长期价值的事实保留在 `.wiki`；过程性证据留在 `.spec`/archive。
6. upstream 合并前后重新核对 `upstream/main` SHA、migration checksum、生成文件漂移和本页覆盖情况；不得用删除 Wiki 记录来掩盖未登记业务差异。

## Skill 合同映射

### `sub2api-fork-extension-audit`

- 只读检查，不执行 fetch、merge、checkout、reset、代码生成、构建、VM Gate 或生产操作。
- 审计必须使用操作者提供的 40 位官方目标 SHA；短 SHA、未知对象、脏工作区、冲突和 catalog 不可读均为 blocker。
- `extensions.yaml` 是唯一机器可读事实源；报告只写 `.tmp/fork-extension-audit/`。
- `catalog_update_required` 不能通过扩大通配符隐藏，必须补齐扩展 ID、路径、迁移、invariants 和最低测试。
- 高风险冲突不得用整文件 ours/theirs 解决；官方修复覆盖关系按故障域分别判断。
- pre/post merge 会对两侧共同修改的 UI 和高风险文件生成保守的 `semantic_overlap_candidate` warning。候选证据来自新增代码中的共同入口、标识符或 catalog 显式规则，只用于提示人工按“完整覆盖、部分覆盖、相邻独立、无法确定”裁决；它不判定等价，不自动删除或回退 fork 实现，显式规则也不能替代完整实现和回归测试证据。
- 已采用但未正式发布的 upstream 批次必须保持可达；VERSION 未变化时不提前升版，目标正式版本前只临时登记批次路径，到达后撤销临时所有权并先完成逐提交归属重核。

### `sub2api-production-deploy`

- Fork 版本严格为官方正式版本加 `-baiyu`；通常读取官方源码 `VERSION`，正式 tag 仅在构建阶段改写版本时必须按目标 commit 显式固定 release version。fork VERSION 每变化一次都新增下一个连续整数 profile，parent 指向此前当前 profile，即使无新增 migration也创建 `new_migrations=[]` 的新 profile；历史 profile、migration、checksum 和 compatibility identity 不回写。profile 242–253 保持既有历史合同；当前 pending profile 254 使用 `0.2.7-baiyu`、parent 253，并新增 migration `278_fork_group_ttft_guard_policies.sql` 与 `279_proxy_ip_groups.sql`。
- 官方 migration 编号若与 fork 已发布历史冲突，必须保留 SQL 内容并改用下一段本地连续编号。profile 245 固定保留官方重编号 `256–259` 及 fork `260–262`；profile 246 只追加 `263_add_usage_log_upstream_request_id.sql`、`264_add_usage_log_upstream_request_id_index_notx.sql`、`265_channel_max_reasoning_effort_multiplier.sql`、`266_group_codex_models_manifest_config.sql`，分别保留官方 `232/233/234` 内容及并发索引非事务语义。profile 247 只追加重编号后的官方 `267_group_model_allowlist.sql`（官方 235）。`255_account_proxy_bindings.sql` 仅作为已发布兼容残留保留，回退后的代码不重新依赖该表; profile 248 adds official migration 236 as local migration 268; profile 249 adds the upstream MiniMax platform migration as local migration 269。
- GPT-6 Astra 的模型识别、默认目录、别名和账号级 live/pinned 能力归 adopted upstream 所有；fork 只保留 provider-qualified 名称与推理等级目录的专项回归测试，不再登记运行时 workaround。Anthropic 桥接、0 倍率、指纹和共享并发是独立合同，不随 Astra 合并回退。
- 账号 compact 列表返回脱敏后的上游身份、能力、调度和动作元数据，不返回凭据；编辑按需加载详情，后台列表刷新不得覆盖完整编辑对象。上游托管白名单同步和普通账号的官方模型元数据同步保持独立，编辑白名单按具体字段扩展。
- 后端、数据库、fork、共享契约和配置改动必须走 `sub2api-dev` VM Gate；本机静态/unit 检查不能替代正式 Gate。
- 正常应用发布使用 active/candidate 双槽和 graceful reload；旧容器在候选构建、传输和验证完成前保持运行，排空超时优先回切旧槽。
- 一个 release 只允许一个 runner、一个 active claim 和一个 candidate；异常、超时或断连先按 committed marker 和结构化状态 reconciliation，禁止重复 deploy。
- 生产清理和备份清理均先 dry-run，再用同一 `plan_sha256` apply；禁止扩大删除范围。
- 生产原始 stdout/stderr 与容器日志只保存在 release 目录 root-only 文件，不复制到 `.tmp`、控制台或最终报告。
- preflight 必须同时核对整体 profile 与单个 migration 状态（`absent/verified/unknown`）；checksum 或语义不一致立即停止。
- Gate v2 当前 profile（现为 254）的升级合同是 health-only；历史 profile 242–253 仅保留用于受控恢复/审计。只验证容器 health、应用 `/health` 和路由，不读取账号池、不发送模型请求、不生成 usage attribution。
- 正式成功必须同时具备签名 Gate、`production-result.json` verified、`verify-result` 通过和 post-deploy doctor 通过；否则只能进入 reconciliation。
- 每次实际发布执行 `fast` 0–2 分钟；migration、PostgreSQL/Redis、Compose、备份、恢复、ingress 或发布状态机变化执行 `specialized` 5–15 分钟；`full` 20–60 分钟至少每 30 天演练一次，不阻塞普通发布，但恢复链、备份格式、恢复信任链变化或真实恢复事故修复时必须在生产前通过。
- Manifest parser/schema、Admin UI API、插件管理路由、宿主声明式渲染器、安全校验和 v1 iframe 兼容修复属于宿主应用变更，不能按独立插件包发布。生产宿主已经实现并验证目标 `admin_ui` 版本后，插件包内声明式定义变化才允许 `plugin-package`；双架构包必须验证定义文件身份和语义一致。
- Redis 恢复允许惰性过期造成总键与 TTL 键不同步下降，但要求恢复总键和 TTL 键不超过备份，且永久键不得减少；checksum、RDB/AOF、认证、启动和 keyspace 可读性仍是独立硬门禁。
- 恢复 orchestrator、restore、cleanup、reconcile 必须来自同一 helper bundle 并记录 SHA-256；恢复按 PostgreSQL、Redis、Compose、应用、Nginx、backup units、claim 和 state cleanup checkpoint 幂等续跑。
- `verify-result` 只验真候选成功上线，`verify-recovery-result` 只验真恢复旧版本；两类验真不可互相替代或放宽。

这两套 skill 的稳定边界需要同步反映到本页；过程命令、单次 release ID、临时日志和测试输出仍留在 `.spec`、`.tmp` 或受控 release 目录，不写入长期 Wiki。

## 维护入口

- 机器可读差异：`.agents/skills/sub2api-fork-extension-audit/references/extensions.yaml`
- 只读审计：`.agents/skills/sub2api-fork-extension-audit/SKILL.md`
- 发布与恢复门禁：`.agents/skills/sub2api-production-deploy/SKILL.md`
- 过程 change：`.spec/changes/**`
- 长期知识：`.wiki/**`
