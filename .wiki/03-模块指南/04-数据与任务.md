---
title: 数据与任务
description: Ent、SQL、Redis、缓存、备份和异步任务
updated: 2026-09-20
owner: backend
---

# 数据与任务

Ent/schema 覆盖用户、组、订阅、API Key、账号、渠道、用量、审计和运维；repository 封装 Ent、原生 SQL 和 Redis。生成 Ent 文件是派生输出，长期合同来自 schema、迁移和 service/repository 接口。

Redis 承载 auth cache、限流、并发、调度快照、Token/session、队列和临时不可用状态。备份、S3/image storage、恢复、定时测试、配额同步和监控任务属于管理面；外部存储、整库覆盖或凭据导出受 step-up 和部署恢复门禁约束。

上游健康观测以 `source=probe` 保存主动探针证据；真实流量证据仍按 `LastEvidenceAt` 参与普通后台探针 freshness 抑制。可信度统计只读取新 Juice 版本且只用有效完成响应，网络异常、HTTP 错误、超时、流中断和无返回不进入分母。旧可信度字段清理必须幂等，并保留同一观测中的普通健康、HTTP、TTFT 和时间证据。相关长期合同见[分叉扩展与兼容性](./05-分叉扩展与兼容性.md)。
## 邮件收件域名路由

系统邮件统一经 `EmailService.SendEmail` 发送。主 SMTP 是默认 profile；管理员可启用收件域名规则（精确域名或 `*.example.com`）并配置独立 QQ SMTP。命中规则且 QQ profile 完整时使用 QQ 发件地址，未命中、路由关闭或 QQ 配置不完整时使用主 SMTP。

QQ SMTP 的连接、TLS、认证、MAIL 或 RCPT 阶段失败可以回退主 SMTP；DATA 阶段已经开始后结果可能已投递，禁止再次发送以避免重复邮件。settings GET 只返回 `smtp_*_password_configured`，空密码 PUT 保留旧值。日志仅记录 profile、阶段和收件域名哈希，不记录凭据、完整地址或正文。

## 管理员账号 JSON 导入

`POST /admin/accounts/data` 每个源账号只创建一条账号记录。只有导入内容全部为 OpenAI OAuth/Setup Token 时才允许统一提交 `proxy_ip_group_id`；选择代理组后忽略导入文件中的单代理绑定。混合平台或包含其它账号类型的批次不得提交非空代理组。

`override_concurrency`、`override_rate_multiplier`、`override_priority` 和 `override_codex_fingerprint_mode` 继续采用“填写即覆盖、未填写保留”规则；并发和倍率允许数值 `0`，优先级必须大于等于 `1`。弹窗默认值为并发 `4`、倍率 `0`、优先级 `1` 和 `device`；用户清空后不提交对应覆盖。单平台导入可同时提交 `group_ids` 与其子集 `preferred_group_ids`，对每个新账号精确设置最终分组和调度优先星标；两个字段必须同时出现，混合平台批次不得提交非空分组选择。

`copy_proxy_ids` 的新建能力已退场，只保留旧协议拒绝护栏。旧客户端提交非空值时固定返回 `COPY_PROXY_IMPORT_DEPRECATED`，不得静默继续复制或退化为单条导入。新导入不再写 `codex_import_replica_fingerprint_seed`，指纹覆盖只为每个新账号生成独立账号级 seed；历史字段不迁移清理、不接受外部覆盖，也不再参与设备、会话或线程身份派生。历史复制账号不自动归并、停用或删除。代理组与账号级 STATE 合同见 [OpenAI OAuth 账号代理组](./09-OpenAI-OAuth账号代理组.md)。
