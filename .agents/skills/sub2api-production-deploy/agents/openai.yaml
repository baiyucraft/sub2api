interface:
  display_name: "Sub2API 部署与运维"
  short_description: "通过持久 runner、VM、备份和双链路门禁发布 Sub2API。"
  default_prompt: "使用 $sub2api-production-deploy，根据最终 diff 分类并读取对应 reference；生产发布使用持久 runner 启动、白名单观察、结果验真和受限恢复入口。"
